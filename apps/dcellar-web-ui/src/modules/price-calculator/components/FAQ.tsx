import { Box, Text } from '@totejs/uikit';
import React from 'react';

export const FAQ = () => {
  return (
    <Box margin={'0 auto'} w={954}>
      <Text fontSize={24} fontWeight={700} mb={24}>
        FAQ
      </Text>
    </Box>
  );
};
