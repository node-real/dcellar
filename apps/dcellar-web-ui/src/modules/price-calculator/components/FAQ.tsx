import { Text } from '@totejs/uikit';
import { PriceResponsiveContainer } from '..';

export const FAQ = () => {
  return (
    <PriceResponsiveContainer margin={'0 auto'}>
      <Text fontSize={24} fontWeight={700} mb={24}>
        FAQ
      </Text>
    </PriceResponsiveContainer>
  );
};
