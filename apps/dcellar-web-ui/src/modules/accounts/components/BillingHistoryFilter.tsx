import { Flex, Text } from '@totejs/uikit';

export const BillingHistoryFilter = () => {
  return (
    <Flex justifyContent={'space-between'}>
      <Text fontSize={16} fontWeight={600} h={40}>
        Billing History
      </Text>
    </Flex>
  );
};
